# Font Styler 🎨

A simple Flutter app that lets you design and style text interactively.  
You can add text, move it around, change fonts (Google Fonts), adjust size, bold, italic, underline, alignments, and even undo/redo changes.

---

## 🚀 Features
- Add and edit custom text  
- Change font styles using Google Fonts  
- Bold / Italic / Underline options  
- Change text alignment (Left, Center, Right)  
- Drag text freely inside the box  
- Undo / Redo support  
- Clean and minimal UI  

---

## 🧠 How to Run This App

1. **Extract the ZIP file**  
   Unzip the folder named `font_styler` (or `FontStyler_Final`).

2. **Open the folder in VS Code or Android Studio**

3. **Run the following commands:**
   ```bash
   flutter pub get
   flutter run
